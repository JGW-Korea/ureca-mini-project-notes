<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.notes.ureca_mini_project_notes.memo.dao.MemoDAO">
  
  <select id="findAll" parameterType="int" resultType="Memo">
    select group_no as groupNo, memo_no as memoNo, title, description as content from memos where group_no = #{no}
  </select>

  <select id="find" parameterType="Memo" resultType="Memo">
    select group_no as groupNo, memo_no as memoNo, title, description as content from memos where group_no = #{groupNo} and title = #{title}
  </select>

  <insert id="create" parameterType="Memo">
    insert into memos (group_no, title, description) values (#{groupNo}, #{title}, #{content})
  </insert>


</mapper>

